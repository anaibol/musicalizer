
console.dev = {};

Object.keys(console).forEach(function(fnName) {
  return console.dev[fnName] = function() {
    if (process.env.NODE_ENV !== "production") {
      return console[fnName].apply(console, arguments);
    }
  };
});
